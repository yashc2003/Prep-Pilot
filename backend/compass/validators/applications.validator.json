{
  "$jsonSchema": {
    "bsonType": "object",
    "required": ["job_id", "candidate_id", "status", "applied_date"],
    "properties": {
      "job_id": { "bsonType": "objectId" },
      "candidate_id": { "bsonType": "objectId" },
      "status": { "bsonType": "string", "enum": ["applied", "shortlisted", "rejected", "selected"] },
      "applied_date": { "bsonType": "date" }
    }
  }
}

